interface:
  display_name: "Agent Note"
  short_description: "Save and retrieve project-scoped notes"
  default_prompt: "Use $agent-note to save this as a project-scoped note."

dependencies:
  tools:
    - type: "mcp"
      value: "agent_note"
      description: "Agent Note MCP server"
