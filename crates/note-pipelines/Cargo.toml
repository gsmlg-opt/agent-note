[package]
name = "note-pipelines"
version = "0.1.0"
edition.workspace = true

[dependencies]
anyhow = "1.0.103"
note-core = { version = "0.1.0", path = "../note-core" }
note-embedding = { version = "0.1.0", path = "../note-embedding" }
note-storage = { version = "0.1.0", path = "../note-storage" }
